# Medical-Management-APi
lightweight, high-performance Medical Management REST API built using the FastAPI framework and backed by a MySQL database. The system handles secure storage and validation of user records using Pydantic schemas.
